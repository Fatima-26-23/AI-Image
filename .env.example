# Vision + embedding provider (cloud path)
GEMINI_API_KEY=your-google-ai-studio-key-here

# Local path (if using Ollama instead of Gemini)
OLLAMA_BASE_URL=http://localhost:11434

DATABASE_URL=postgresql://user:password@localhost:5432/flyrank_capstone

LOW_CONFIDENCE_THRESHOLD=0.6
SIMILARITY_THRESHOLD=0.55
